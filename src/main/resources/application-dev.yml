spring:
  rabbitmq:
    addresses: ${SPRING_RABBITMQ_ADDRESSES:}
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:sqlite:${user.home}/.creatorsdeck/creatorsdeck.db
    driver-class-name: org.sqlite.JDBC
    username: ""
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.community.dialect.SQLiteDialect

app:
  jwt:
    secret: 7d9d2e5d7e0e4d7aa8e5c3b1f6a9c8d7e0f4a1b2c3d4e5f60718293a4b5c6d7
